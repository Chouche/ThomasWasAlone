#include "../include/Camera.h"


/*

int MooveCameraLeft(Personnage perso,Bloc blocs[],int nb_bloc){
	int i,col = 0;
	for (i = 0; i < nb_bloc;i++)
	{
		if( CollisionHG(perso,blocs[i]) == 1 || CollisionBG(perso,blocs[i]) == 1)
			col = 1;
	}
	return col;
}

int MooveCameraRight(Personnage perso,Bloc blocs[],int nb_bloc){
	
	int i,col = 0;
	for (i = 0; i < nb_bloc;i++)
	{
		if(CollisionBD(perso,blocs[i])==1 || CollisionHD(perso,blocs[i])==1)
			{ 
				col = 1;
			}	
	}
	return col;
}
*/